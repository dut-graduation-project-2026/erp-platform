package com.dut.erp.enums;

/**
 * Logical entity types used to categorise audit log entries.
 * Add new values here as new modules are introduced.
 */
public enum EntityType {
  PARTNER,
  PARTNER_CONTACT,
  ORGANIZATION,
  USER,
  ROLE,
  INVITATION,
  ERP_MODULE,
}
